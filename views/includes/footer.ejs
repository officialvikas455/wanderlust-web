<!-- <footer>
    <div class="f-info">
        <div class="f-info-socials">
            <i class="fa-brands fa-square-facebook"></i>
            <i class="fa-brands fa-square-instagram"></i>
            <i class="fa-brands fa-linkedin"></i>

        </div>
        <div class="f-info-brand"> &copy; Wanderlust Private Limited </div>
        <div class="f-info-links">
            <a href="/privacy">Privacy</a>
            <a href="/terms">Terms</a>
        </div>
    </div>
</footer> -->



<footer class="site-footer">

    <!-- Animated star particles -->
    <canvas class="footer-canvas" id="footerCanvas" aria-hidden="true"></canvas>
  
    <!-- Top wave divider -->
    <div class="footer-wave" aria-hidden="true">
      <svg viewBox="0 0 1440 80" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M0,40 C240,80 480,0 720,40 C960,80 1200,0 1440,40 L1440,80 L0,80 Z" fill="#0a0a0c"/>
      </svg>
    </div>
  
    <div class="footer-wrap">
  
      <!-- ══ NEWSLETTER BAND ══ -->
      <div class="newsletter-band">
        <div class="newsletter-text">
          <span class="nl-eyebrow"><i class="fa-solid fa-paper-plane"></i> Stay inspired</span>
          <h3 class="nl-heading">The world is waiting — <em>don't miss a stay</em></h3>
        </div>
        <div class="newsletter-form">
          <div class="nl-input-wrap">
            <i class="fa-regular fa-envelope nl-icon"></i>
            <input type="email" placeholder="your@email.com" class="nl-input" />
            <button class="nl-btn">Subscribe</button>
          </div>
          <p class="nl-note">No spam, ever. Unsubscribe anytime.</p>
        </div>
      </div>
  
      <!-- ══ MAIN FOOTER GRID ══ -->
      <div class="footer-grid">
  
        <!-- Brand col -->
        <div class="footer-col footer-col--brand">
          <div class="brand-mark">
            <div class="brand-icon">
              <i class="fa-solid fa-compass"></i>
            </div>
            <div class="brand-words">
              <span class="brand-name">Wanderlust</span>
              <span class="brand-sub">Private Limited</span>
            </div>
          </div>
          <p class="brand-bio">
            Curating extraordinary stays across India — from misty mountain retreats
            to hidden coastal gems. Every listing, a story.
          </p>
          <div class="brand-stats">
            <div class="stat">
              <span class="stat-num">12k+</span>
              <span class="stat-lbl">Listings</span>
            </div>
            <div class="stat-divider"></div>
            <div class="stat">
              <span class="stat-num">4.9★</span>
              <span class="stat-lbl">Avg Rating</span>
            </div>
            <div class="stat-divider"></div>
            <div class="stat">
              <span class="stat-num">98%</span>
              <span class="stat-lbl">Happy Guests</span>
            </div>
          </div>
        </div>
  
        <!-- Explore col -->
        <div class="footer-col">
          <h4 class="col-heading">Explore</h4>
          <ul class="col-links">
            <li><a href="/listings"><i class="fa-solid fa-chevron-right"></i> All Listings</a></li>
            <li><a href="/listings/new"><i class="fa-solid fa-chevron-right"></i> Add a Place</a></li>
            <li><a href="/"><i class="fa-solid fa-chevron-right"></i> Trending Stays</a></li>
            <li><a href="/"><i class="fa-solid fa-chevron-right"></i> Mountains</a></li>
            <li><a href="/"><i class="fa-solid fa-chevron-right"></i> Beach Stays</a></li>
          </ul>
        </div>
  
        <!-- Company col -->
        <div class="footer-col">
          <h4 class="col-heading">Company</h4>
          <ul class="col-links">
            <li><a href="/about"><i class="fa-solid fa-chevron-right"></i> About Us</a></li>
            <li><a href="/careers"><i class="fa-solid fa-chevron-right"></i> Careers</a></li>
            <li><a href="/press"><i class="fa-solid fa-chevron-right"></i> Press Kit</a></li>
            <li><a href="/privacy"><i class="fa-solid fa-chevron-right"></i> Privacy Policy</a></li>
            <li><a href="/terms"><i class="fa-solid fa-chevron-right"></i> Terms of Service</a></li>
          </ul>
        </div>
  
        <!-- Support col -->
        <div class="footer-col">
          <h4 class="col-heading">Support</h4>
          <ul class="col-links">
            <li><a href="/help"><i class="fa-solid fa-chevron-right"></i> Help Center</a></li>
            <li><a href="/contact"><i class="fa-solid fa-chevron-right"></i> Contact Us</a></li>
            <li><a href="/faq"><i class="fa-solid fa-chevron-right"></i> FAQ</a></li>
            <li><a href="/safety"><i class="fa-solid fa-chevron-right"></i> Safety</a></li>
            <li><a href="/accessibility"><i class="fa-solid fa-chevron-right"></i> Accessibility</a></li>
          </ul>
        </div>
  
      </div>
  
      <!-- ══ DIVIDER ══ -->
      <div class="footer-hr">
        <div class="hr-line"></div>
        <div class="hr-center">
          <i class="fa-solid fa-compass hr-compass"></i>
        </div>
        <div class="hr-line"></div>
      </div>
  
      <!-- ══ BOTTOM BAR ══ -->
      <div class="footer-bottom">
  
        <p class="footer-copy">
          &copy; <span id="f-year"></span> Wanderlust Private Limited &mdash; Made with
          <i class="fa-solid fa-heart" style="color:#e8a838;font-size:0.7rem;"></i> in India
        </p>
  
        <!-- Socials -->
        <div class="footer-socials">
          <a href="#" class="soc" aria-label="Facebook" data-tooltip="Facebook">
            <i class="fa-brands fa-facebook-f"></i>
          </a>
          <a href="#" class="soc" aria-label="Instagram" data-tooltip="Instagram">
            <i class="fa-brands fa-instagram"></i>
          </a>
          <a href="#" class="soc" aria-label="LinkedIn" data-tooltip="LinkedIn">
            <i class="fa-brands fa-linkedin-in"></i>
          </a>
          <a href="#" class="soc" aria-label="X" data-tooltip="X / Twitter">
            <i class="fa-brands fa-x-twitter"></i>
          </a>
          <a href="#" class="soc" aria-label="YouTube" data-tooltip="YouTube">
            <i class="fa-brands fa-youtube"></i>
          </a>
        </div>
  
        <!-- Badges -->
        <div class="footer-badges">
          <div class="badge-pill">
            <i class="fa-solid fa-leaf"></i> Carbon Neutral
          </div>
          <div class="badge-pill">
            <i class="fa-solid fa-shield-halved"></i> Secure Payments
          </div>
        </div>
  
      </div>
  
    </div><!-- /footer-wrap -->
  </footer>
  
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Italiana&family=Outfit:wght@300;400;500;600&display=swap');
  
    /* ══════════════════════════════════
       FOOTER BASE
    ══════════════════════════════════ */
    .site-footer {
      position: relative;
      background: #0a0a0c;
      font-family: 'Outfit', sans-serif;
      overflow: hidden;
      margin-top: 6rem;
    }
  
    /* Canvas particles */
    .footer-canvas {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      opacity: 0.5;
    }
  
    /* Wave top */
    .footer-wave {
      position: absolute;
      top: -1px;
      left: 0; right: 0;
      height: 80px;
      line-height: 0;
    }
    .footer-wave svg {
      width: 100%;
      height: 100%;
      display: block;
    }
  
    .footer-wrap {
      position: relative;
      z-index: 2;
      max-width: 1240px;
      margin: 0 auto;
      padding: 5rem 2rem 2.5rem;
    }
  
    /* ══════════════════════════════════
       NEWSLETTER BAND
    ══════════════════════════════════ */
    .newsletter-band {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 2rem;
      flex-wrap: wrap;
      background: linear-gradient(135deg,
        rgba(232,168,56,0.08) 0%,
        rgba(232,168,56,0.04) 50%,
        rgba(99,102,241,0.06) 100%
      );
      border: 1px solid rgba(232,168,56,0.15);
      border-radius: 20px;
      padding: 2rem 2.4rem;
      margin-bottom: 4rem;
      position: relative;
      overflow: hidden;
    }
    .newsletter-band::before {
      content: '';
      position: absolute;
      top: -40px; right: -40px;
      width: 200px; height: 200px;
      background: radial-gradient(circle, rgba(232,168,56,0.1), transparent 70%);
      pointer-events: none;
    }
  
    .nl-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      font-size: 0.65rem;
      font-weight: 600;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: #e8a838;
      margin-bottom: 0.5rem;
    }
    .nl-heading {
      font-family: 'Italiana', serif;
      font-size: clamp(1.3rem, 2.5vw, 1.8rem);
      font-weight: 400;
      color: #f0ece4;
      line-height: 1.2;
    }
    .nl-heading em { color: #e8a838; font-style: italic; }
  
    .newsletter-form { flex-shrink: 0; min-width: 300px; }
    .nl-input-wrap {
      display: flex;
      align-items: center;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 50px;
      padding: 0.3rem 0.3rem 0.3rem 1rem;
      gap: 0.5rem;
      transition: border-color 0.2s, box-shadow 0.2s;
    }
    .nl-input-wrap:focus-within {
      border-color: rgba(232,168,56,0.5);
      box-shadow: 0 0 0 3px rgba(232,168,56,0.1);
    }
    .nl-icon { color: #6b6860; font-size: 0.8rem; flex-shrink: 0; }
    .nl-input {
      flex: 1;
      background: none;
      border: none;
      outline: none;
      font-family: 'Outfit', sans-serif;
      font-size: 0.82rem;
      color: #f0ece4;
      min-width: 0;
    }
    .nl-input::placeholder { color: #4a4845; }
    .nl-btn {
      font-family: 'Outfit', sans-serif;
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.06em;
      color: #0a0a0c;
      background: linear-gradient(135deg, #e8a838, #f5c842);
      border: none;
      border-radius: 50px;
      padding: 0.6rem 1.3rem;
      cursor: pointer;
      white-space: nowrap;
      transition: filter 0.2s, transform 0.15s;
      flex-shrink: 0;
    }
    .nl-btn:hover { filter: brightness(1.1); transform: scale(1.03); }
    .nl-note {
      font-size: 0.65rem;
      color: #4a4845;
      margin-top: 0.5rem;
      padding-left: 0.5rem;
      font-weight: 300;
    }
  
    /* ══════════════════════════════════
       MAIN GRID
    ══════════════════════════════════ */
    .footer-grid {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 1fr;
      gap: 3rem;
      margin-bottom: 3.5rem;
    }
  
    /* Brand col */
    .brand-mark {
      display: flex;
      align-items: center;
      gap: 0.9rem;
      margin-bottom: 1.2rem;
    }
    .brand-icon {
      width: 48px; height: 48px;
      border-radius: 14px;
      background: linear-gradient(135deg, #e8a838 0%, #f5c842 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
      color: #0a0a0c;
      box-shadow: 0 6px 20px rgba(232,168,56,0.35);
      flex-shrink: 0;
      position: relative;
    }
    .brand-icon::after {
      content: '';
      position: absolute;
      inset: -1px;
      border-radius: 15px;
      background: linear-gradient(135deg, #f5c842, transparent);
      opacity: 0.4;
      pointer-events: none;
    }
    .brand-name {
      display: block;
      font-family: 'Italiana', serif;
      font-size: 1.5rem;
      color: #f0ece4;
      line-height: 1;
      letter-spacing: 0.02em;
    }
    .brand-sub {
      display: block;
      font-size: 0.6rem;
      color: #4a4845;
      font-weight: 300;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      margin-top: 0.15rem;
    }
    .brand-bio {
      font-size: 0.8rem;
      color: #5a5650;
      line-height: 1.8;
      font-weight: 300;
      margin-bottom: 1.5rem;
      max-width: 280px;
    }
  
    /* Stats row */
    .brand-stats {
      display: flex;
      align-items: center;
      gap: 1.2rem;
    }
    .stat { text-align: center; }
    .stat-num {
      display: block;
      font-size: 1rem;
      font-weight: 600;
      color: #e8a838;
      line-height: 1;
      margin-bottom: 0.2rem;
    }
    .stat-lbl {
      font-size: 0.62rem;
      color: #4a4845;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      font-weight: 400;
    }
    .stat-divider {
      width: 1px;
      height: 28px;
      background: rgba(255,255,255,0.07);
    }
  
    /* Nav columns */
    .col-heading {
      font-size: 0.65rem;
      font-weight: 600;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: #e8a838;
      margin-bottom: 1.2rem;
      position: relative;
      padding-bottom: 0.7rem;
    }
    .col-heading::after {
      content: '';
      position: absolute;
      bottom: 0; left: 0;
      width: 20px; height: 1.5px;
      background: linear-gradient(90deg, #e8a838, transparent);
      border-radius: 2px;
    }
    .col-links {
      list-style: none;
      padding: 0; margin: 0;
      display: flex;
      flex-direction: column;
      gap: 0.65rem;
    }
    .col-links a {
      font-size: 0.8rem;
      color: #5a5650;
      text-decoration: none;
      font-weight: 300;
      display: flex;
      align-items: center;
      gap: 0.45rem;
      transition: color 0.2s, gap 0.2s;
      letter-spacing: 0.01em;
    }
    .col-links a i {
      font-size: 0.45rem;
      color: #3a3835;
      transition: color 0.2s, transform 0.2s;
      flex-shrink: 0;
    }
    .col-links a:hover { color: #f0ece4; gap: 0.65rem; }
    .col-links a:hover i { color: #e8a838; transform: translateX(2px); }
  
    /* ══════════════════════════════════
       DIVIDER
    ══════════════════════════════════ */
    .footer-hr {
      display: flex;
      align-items: center;
      gap: 1.5rem;
      margin-bottom: 2rem;
    }
    .hr-line {
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.06), transparent);
    }
    .hr-center {
      width: 38px; height: 38px;
      border-radius: 50%;
      border: 1px solid rgba(232,168,56,0.2);
      background: rgba(232,168,56,0.06);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }
    .hr-compass {
      font-size: 0.85rem;
      color: #e8a838;
      animation: spin 20s linear infinite;
    }
    @keyframes spin { to { transform: rotate(360deg); } }
  
    /* ══════════════════════════════════
       BOTTOM BAR
    ══════════════════════════════════ */
    .footer-bottom {
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 1.2rem;
    }
  
    .footer-copy {
      font-size: 0.73rem;
      color: #3a3835;
      font-weight: 300;
      letter-spacing: 0.02em;
      display: flex;
      align-items: center;
      gap: 0.3rem;
    }
  
    /* Socials */
    .footer-socials {
      display: flex;
      gap: 0.5rem;
    }
    .soc {
      width: 36px; height: 36px;
      border-radius: 10px;
      border: 1px solid rgba(255,255,255,0.06);
      background: rgba(255,255,255,0.03);
      display: flex;
      align-items: center;
      justify-content: center;
      color: #4a4845;
      font-size: 0.78rem;
      text-decoration: none;
      transition: all 0.22s cubic-bezier(0.34,1.56,0.64,1);
      position: relative;
    }
    .soc::after {
      content: attr(data-tooltip);
      position: absolute;
      bottom: calc(100% + 8px);
      left: 50%;
      transform: translateX(-50%) scale(0.8);
      background: rgba(232,168,56,0.95);
      color: #0a0a0c;
      font-size: 0.6rem;
      font-weight: 600;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
      white-space: nowrap;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.15s, transform 0.15s;
      font-family: 'Outfit', sans-serif;
      letter-spacing: 0.04em;
    }
    .soc:hover {
      background: rgba(232,168,56,0.12);
      border-color: rgba(232,168,56,0.35);
      color: #e8a838;
      transform: translateY(-3px);
      box-shadow: 0 6px 16px rgba(232,168,56,0.18);
    }
    .soc:hover::after {
      opacity: 1;
      transform: translateX(-50%) scale(1);
    }
  
    /* Badges */
    .footer-badges {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }
    .badge-pill {
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      font-size: 0.64rem;
      color: #4a4845;
      font-weight: 400;
      border: 1px solid rgba(255,255,255,0.05);
      border-radius: 100px;
      padding: 0.28rem 0.7rem;
      background: rgba(255,255,255,0.02);
      letter-spacing: 0.04em;
      transition: border-color 0.2s, color 0.2s;
    }
    .badge-pill:hover { border-color: rgba(232,168,56,0.2); color: #8a8278; }
    .badge-pill i { font-size: 0.55rem; color: #4ade80; }
    .badge-pill:last-child i { color: #60a5fa; }
  
    /* ══════════════════════════════════
       RESPONSIVE
    ══════════════════════════════════ */
    @media (max-width: 1024px) {
      .footer-grid { grid-template-columns: 1.5fr 1fr 1fr; }
      .footer-col--brand { grid-column: 1 / -1; }
      .brand-bio { max-width: 100%; }
    }
    @media (max-width: 768px) {
      .footer-grid { grid-template-columns: 1fr 1fr; gap: 2rem; }
      .footer-col--brand { grid-column: 1 / -1; }
      .newsletter-band { flex-direction: column; align-items: flex-start; }
      .newsletter-form { width: 100%; }
      .nl-input-wrap { width: 100%; }
      .footer-bottom { justify-content: center; text-align: center; }
      .footer-copy { justify-content: center; }
    }
    @media (max-width: 480px) {
      .footer-wrap { padding: 4rem 1.2rem 2rem; }
      .footer-grid { grid-template-columns: 1fr; }
      .brand-stats { gap: 0.9rem; }
      .footer-socials { flex-wrap: wrap; justify-content: center; }
      .footer-badges { justify-content: center; }
    }
  </style>
  
  <script>
  (function() {
    /* Auto year */
    document.getElementById("f-year").textContent = new Date().getFullYear();
  
    /* Particle canvas */
    const canvas = document.getElementById("footerCanvas");
    if (!canvas) return;
    const ctx = canvas.getContext("2d");
    let W, H, particles = [];
  
    function resize() {
      W = canvas.width  = canvas.offsetWidth;
      H = canvas.height = canvas.offsetHeight;
    }
    resize();
    window.addEventListener("resize", resize);
  
    function randomParticle() {
      return {
        x: Math.random() * W,
        y: Math.random() * H,
        r: Math.random() * 1.2 + 0.3,
        vx: (Math.random() - 0.5) * 0.18,
        vy: (Math.random() - 0.5) * 0.18,
        alpha: Math.random() * 0.5 + 0.1,
        color: Math.random() > 0.7 ? "#e8a838" : "#ffffff"
      };
    }
  
    for (let i = 0; i < 90; i++) particles.push(randomParticle());
  
    function draw() {
      ctx.clearRect(0, 0, W, H);
      particles.forEach(p => {
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        ctx.fillStyle = p.color;
        ctx.globalAlpha = p.alpha;
        ctx.fill();
        p.x += p.vx;
        p.y += p.vy;
        if (p.x < 0 || p.x > W) p.vx *= -1;
        if (p.y < 0 || p.y > H) p.vy *= -1;
      });
      ctx.globalAlpha = 1;
      requestAnimationFrame(draw);
    }
    draw();
  })();
  </script>